
# 🛡️ Spring Security: Access + Refresh Tokens с Redis

Это приложение реализует **современную, безопасную и масштабируемую систему аутентификации** на основе двух типов JWT-токенов:

- **Access Token** — короткоживущий (например, 30 минут), используется для авторизации запросов к защищённым API.
- **Refresh Token** — долгоживущий (например, 30 дней), хранится в **Redis** и применяется для получения новой пары токенов **без повторного ввода логина и пароля**.

---

### 1. **Регистрация**
- Запрос: `POST /api/v1/auth/register`
  ```json
  { "email": "user@example.com", "name": "Alex", "password1": "secure123" }
  ```
- Ответ: `201 Created`
  ```json
  { "timestamp": "2025-11-09T10:00:00", "message": "Пользователь успешно создан" }
  ```

### 2. **Аутентификация**
- Запрос: `POST /api/v1/auth/login`
  ```json
  { "email": "user@example.com", "password": "secure123" }
  ```
- Ответ: `200 OK`
  ```json
  {
    "id": 123,
    "timestamp": "2025-11-09T10:00:05",
    "accessToken": "eyJhbGciOiJIUzUxMiIsInR5cCI6IkpXVCJ9.xxxxx",
    "refreshToken": "f3b8a1c4-9d2e-4e5f-8a7b-1c2d3e4f5a6b",
    "email": "user@example.com",
    "name": "Alex",
    "roles": ["ROLE_USER"]
  }
  ```
> ✅ **Refresh Token — это UUID, хранящийся в Redis**, а не JWT.

### 3. **Доступ к защищённым ресурсам**
- Все запросы должны содержать:
  ```
  Authorization: Bearer <accessToken>
  ```

### 4. **Обновление токенов**
- Когда Access Token истекает — клиент отправляет:
  ```
  POST /api/v1/auth/refresh
  { 
  "refreshToken": "f3b8a1c4-9d2e-4e5f-8a7b-1c2d3e4f5a6b"
   }
  ```
- Сервер проверяет:
    - Существует ли токен в Redis.
    - Не истёк ли его срок (`expiryDate`).
- При успехе возвращает **новую пару токенов**, а старый Refresh Token **автоматически удаляется**.

### 5. **Выход из системы**
- Запрос: `POST /api/v1/auth/logout`
- Сервер **удаляет Refresh Token из Redis по `userId`**, мгновенно завершая сессию на всех устройствах.

---

## ⚙️ Технологии и архитектура

- **Spring Boot 3.5+** с Jakarta EE 10
- **Spring Security** — управление аутентификацией и авторизацией
- **JWT (JJWT)** — генерация и валидация Access Token
- **Redis** — хранение Refresh Token с TTL (time-to-live) и индексацией по `userId` и `token`
- **Lombok** — уменьшение шаблонного кода
- **Record-based DTO** — неизменяемые и читаемые объекты ответов/запросов

- 🔒 **Высокая безопасность**: короткий срок жизни Access Token + централизованное управление Refresh Token.
- 🔄 **Надёжный UX**: пользователи не вводят пароль повторно в течение срока действия Refresh Token.
- 🧩 **Готово к микросервисам**: Redis — общий источник истины для всех инстансов.
- 🗑️ **Автоматическая очистка**: устаревшие токены удаляются Redis без участия приложения.
- 🛑 **Мгновенный логаут**: сессия завершается на всех устройствах одним запросом.

---


---

Готово к интеграции в  Spring-микросервисы! 🚀  
Полностью совместимо с **Spring Cloud Gateway**, **Redis Sentinel/Cluster** и **Kubernetes**.

---

## Контакты ✉
Если нужно — пишите: **krp77@mail.ru**