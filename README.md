
# 🛡️ Аутентификация с Access/Refresh Token и CAPTCHA

Приложение использует простую, но безопасную схему входа и регистрации:

- **Access Token** — живёт недолго (30 минут) и нужен для доступа к защищённым API.  
  Благодаря ему пользователь не вводит пароль каждые несколько минут — это улучшает UX.

- **Refresh Token** — живёт долго (до 30 дней), хранится в **Redis**, и позволяет **обновлять сессию без повторного входа**.  
  При выходе из системы он удаляется — сессия завершается на всех устройствах.

- **CAPTCHA** — обязательна при регистрации.  
  Это защищает от ботов: пользователь решает капчу, получает временный токен, и только с ним может зарегистрироваться.

---

### Как это работает

1. **Получить CAPTCHA**  
   `GET /api/v1/captcha` → возвращает картинку и `captchaId`.

2. **Решить CAPTCHA**  
   `POST /api/v1/captcha/solve` → возвращает `captchaToken`.

3. **Зарегистрироваться**  
   `POST /api/v1/auth/register` с `captchaToken` в теле → создаёт аккаунт.

4. **Войти**  
   `POST /api/v1/auth/login` → выдаёт `accessToken` и `refreshToken`.

5. **Работать с API**  
   Все запросы к защищённым эндпоинтам — с заголовком:  
   `Authorization: Bearer <accessToken>`

6. **Обновить сессию**  
   Когда `accessToken` истёк — отправляется `refreshToken` на `/refresh` → выдаётся новая пара токенов.

7. **Выйти**  
   `POST /api/v1/auth/logout` → удаляет `refreshToken` из Redis.

---

### Технологии

- **Spring Boot 3.5+**, **Spring Security**
- **JWT** (через `jjwt`) — для access token
- **Redis** — для хранения refresh token и CAPTCHA-данных
- **Kaptcha** — генерация CAPTCHA-изображений
- **Lombok** и **record-классы** — меньше шаблонного кода

---

### Плюсы

- 🔒 **Безопасно**: короткий access token, проверка CAPTCHA, хранение refresh token в Redis.
- 🔄 **Удобно**: не нужно логиниться каждый раз — сессия обновляется автоматически.
- 🧹 **Чисто**: нет мусора в Redis — всё удаляется по TTL или при logout.
- 📦 **Готово к масштабированию**: Redis работает как общий бэкенд для всех инстансов.

---

Готово к использованию в микросервисах.  
Совместимо с **Spring Cloud Gateway**, **Redis Cluster**.

---

✉ **Контакты**: krp77@mail.ru